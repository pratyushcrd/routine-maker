import { } from 'constants/action-types'

const initialState = {
  days: [{
    day: 'Monday',
    periods: 6
  }]
}

export default function input(state = initialState, action) {
  switch (action.type) {
  default:
    return state
  }
}
